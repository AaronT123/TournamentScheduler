@model IEnumerable<TournamentScheduler.Models.RRFixture>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tournament.TournamentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team1ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team2ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team1Score)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team2Score)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tournament.TournamentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team1ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team2ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team1Score)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team2Score)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RRFixtureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RRFixtureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RRFixtureID })
        </td>
    </tr>
}

</table>
